apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app-targeting-service
    {{- include "togglemaster.labels" .| nindent 4 }}
  name: app-targeting-service
  namespace: {{ (index .Values "targeting-service").namespace }}
spec:
  strategy:
    type: Recreate
  replicas: {{ (index .Values "targeting-service").replicas}}
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: app-targeting-service
  template:
    metadata:
      labels:
        app: app-targeting-service
    spec:
      containers:
        - name: app-targeting-service
          image: {{ (index .Values "targeting-service").image }}
          imagePullPolicy: {{ (index .Values "targeting-service").imagePullPolicy }}
          env:
            - name: POSTGRES_DB
              value: {{ (index .Values "targeting-service").env_vars }}
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          startupProbe:
           httpGet:
             path: /health
             port: {{ (index .Values "targeting-service").port }}
           failureThreshold: 60
           periodSeconds: 5
           timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ (index .Values "targeting-service").port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: {{ (index .Values "targeting-service").port }}
            initialDelaySeconds: 180
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          envFrom:
            {{- toYaml (index .Values "targeting-service").envFrom | nindent 12 }}
