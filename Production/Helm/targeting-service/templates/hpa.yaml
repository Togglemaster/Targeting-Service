{{- if (index .Values "targeting-service").autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: targeting-service-hpa
  namespace: {{ (index .Values "targeting-service").namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: app-targeting-service
  minReplicas: {{ (index .Values "targeting-service").autoscaling.minReplicas }}
  maxReplicas: {{ (index .Values "targeting-service").autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ (index .Values "targeting-service").autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ (index .Values "targeting-service").autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
